package String;
//using length() Method
public class StringLenth {
	public static void main(String[] args) {
		String s="Anudip foundation";
		// one way to find string length
		System.out.println("the string lenght of"+s+"is: " +s.length());
	 
		// get the length of string by another way
		int len=s.length();// length of the string 
		
		System.out.println("the string lenght of"+s+"is: " +len);
		 
	}

}
